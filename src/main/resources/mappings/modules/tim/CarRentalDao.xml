<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ulplanet.trip.modules.tim.dao.CarRentalDao">

    <sql id="carRentalColumns">
        a.id,
        a.country AS 'country.id',
        c.name AS 'country.name',
        a.type,
        a.phone
    </sql>
	<select id="findList" resultType="CarRental">
		SELECT
            <include refid="carRentalColumns"/>
        FROM car_rental a
        JOIN country c
        ON a.country = c.id
        <where>
            <if test="type != null and type != ''">
                AND a.type = #{type}
            </if>
            <if test="country != null and country.id != null and country.id != ''">
                AND a.country = #{country.id}
            </if>
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.country, a.type
            </otherwise>
        </choose>
	</select>
    <select id="get" resultType="CarRental">
        SELECT
        <include refid="carRentalColumns"/>
        FROM car_rental a
        JOIN country c
        ON a.country = c.id
        WHERE a.id = #{id}
    </select>
	<insert id="insert">
        INSERT INTO car_rental(
            id,
            country,
            type,
            phone,
            create_by,
            create_date,
            update_by,
            update_date,
            remarks
        ) VALUES (
            #{id},
            #{country.id},
            #{type},
            #{phone},
            #{createBy.id},
            #{createDate},
            #{updateBy.id},
            #{updateDate},
            #{remarks}
        )
	</insert>
	
	<update id="update">
        UPDATE car_rental SET
            type = #{type},
            country = #{country.id},
            phone = #{phone},
            update_by = #{updateBy.id},
            update_date = #{updateDate},
            remarks = #{remarks}
        WHERE id = #{id}
	</update>
	<delete id="delete">
        DELETE FROM car_rental WHERE id = #{id}
	</delete>
</mapper>