<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ulplanet.trip.modules.tms.dao.GroupHotelDao">
    <resultMap id="GroupHotel" type="com.ulplanet.trip.modules.tms.entity.GroupHotel">
        <result property="id" column="id"/>
        <result property="groupId" column="group_id"/>
        <result property="hotelId" column="hotel_id"/>
        <result property="checkDate" column="check_date"/>
        <result property="common" column="common"/>
        <result property="remarks" column="remarks"/>
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="BaseColumns">
        <![CDATA[
            id,group_id,hotel_id,check_date,common,remarks
	    ]]>
    </sql>

    <sql id="BaseWhere">
        <where>
            <if test="id != null and id != ''">
                AND id = #{id}
            </if>
            <if test="groupId != null and groupId != ''">
                AND group_id = #{groupId}
            </if>
            <if test="hotelId != null and hotelId != ''">
                AND hotel_id = #{hotelId}
            </if>
            <if test="checkDate != null and checkDate != ''">
                AND check_date = #{checkDate}
            </if>
            <if test="common != null and common != ''">
                AND common = #{common}
            </if>
            <if test="remarks != null and remarks != ''">
                AND remarks = #{remarks}
            </if>
        </where>

    </sql>


    <insert id="insert">
        <![CDATA[
                INSERT INTO group_hotel (
                    id ,
                    group_id,
                    hotel_id ,
                    check_date ,
                    common ,
                    create_by ,
                    create_date ,
                    remarks
                ) VALUES (
                    #{id} ,
                    #{groupId},
                    #{hotelId} ,
                    #{checkDate} ,
                    #{common} ,
                    #{createBy.id} ,
                    #{createDate} ,
                    #{remarks}
                )
            ]]>

    </insert>

    <update id="update" >
        UPDATE group_hotel
        <set>
            <if test="groupId != null">
                group_id = #{groupId},
            </if>
            <if test="hotelId != null">
                hotel_id = #{hotelId},
            </if>
            <if test="checkDate != null">
                check_date = #{checkDate},
            </if>
            <if test="common != null">
                common = #{common},
            </if>
            <if test="updateBy.id != null">
                update_by = #{updateBy.id},
            </if>
            <if test="updateDate != null">
                update_date = #{updateDate},
            </if>
            <if test="remarks != null">
                remarks = #{remarks}
            </if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete">
        <![CDATA[
            DELETE FROM group_hotel WHERE
            id = #{id}
        ]]>
    </delete>

    <select id="getById" resultMap="GroupHotel">
        SELECT <include refid="BaseColumns" />
        <![CDATA[
		    FROM group_hotel
	        WHERE
		        id = #{id}
	    ]]>
    </select>

    <select id="findList" resultMap="GroupHotel">
        SELECT <include refid="BaseColumns" />
        FROM group_hotel
        <include refid="BaseWhere"/>
    </select>
</mapper>