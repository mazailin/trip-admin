<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ulplanet.trip.modules.tms.dao.QingmaDao">
    <insert id="insert">
        INSERT INTO qingma_client
        (
          id,
          client_number,
          client_pwd,
          create_time
        )VALUES
        (
          #{id},
          #{clientNumber},
          #{clientPwd},
          #{createTime}
        )
    </insert>

    <update id="update">
        UPDATE qingma_client SET
        status = #{status}
        WHERE id = #{id}
    </update>

    <select id="get" resultType="com.ulplanet.trip.modules.tms.entity.Qingma">
        SELECT id,client_number,client_pwd,create_time
        FROM qingma_client
        WHERE id = #{id} AND status = 1
    </select>



</mapper>