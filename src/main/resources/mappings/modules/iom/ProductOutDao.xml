<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ulplanet.trip.modules.iom.dao.ProductOutDao">

    <sql id="productOutColumns">
        a.`id`,
        a.`code`,
        a.`out_date`,
        a.`out_type`,
        a.`operator`,
        a.`comment`
    </sql>
	<select id="findList" resultType="ProductOut">
		SELECT
            <include refid="productOutColumns"/>
        FROM product_out a
        WHERE a.del_flag = #{DEL_FLAG_NORMAL}
        <if test="code != null and code != ''">
            AND a.code like CONCAT('%', #{code}, '%')
        </if>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.code
            </otherwise>
        </choose>
	</select>
    <select id="get" resultType="ProductOut">
        SELECT
          <include refid="productOutColumns"/>
        FROM product_out a
        WHERE a.id = #{id}
    </select>
	<insert id="insert">
        INSERT INTO product_out(
            `id`,
            `code`,
            `out_date`,
            `out_type`,
            `operator`,
            `comment`,
            `create_by`,
            `create_date`,
            `update_by`,
            `update_date`,
            `remarks`,
            `del_flag`
        ) VALUES (
            #{id},
            #{code},
            #{outDate},
            #{outType},
            #{operator},
            #{comment},
            #{createBy.id},
            #{createDate},
            #{updateBy.id},
            #{updateDate},
            #{remarks},
            #{delFlag}
        )
	</insert>
	
	<update id="update">
        UPDATE product_out SET
            `out_date` = #{outDate},
            `out_type` = #{outType},
            `operator` = #{operator},
            `comment` = #{comment},
            `update_by` = #{updateBy.id},
            `update_date` = #{updateDate},
            `remarks` = #{remarks}
        WHERE id = #{id}
	</update>
	<delete id="delete">
		UPDATE product_out SET del_flag = #{DEL_FLAG_DELETE} WHERE id = #{id}
	</delete>
</mapper>